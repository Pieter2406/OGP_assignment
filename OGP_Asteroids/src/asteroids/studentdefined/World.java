package asteroids.studentdefined;

public class World {
//TODO: write class world
	/*
	 * FULLY DEFENSIVE!
	 * 
	 * Contains following Objects:
	 		* Ship
	  		* Asteroid
	  		* Bullet
	 * Contains following variables:
	 		* width
	 		* height
	 		* maxWidthHeight = Double.MaxValue
	 * Contains following methods:
	 		* addShip(Coordinate initLocation, Velocity initVelocity, double angle)
	 		* addShip(double x, double y, double xVelocity, double yVelocity, double angle)
	 		* ...  
	 */
	
	
}
